{
  "groupId": "auth",
  "testedAt": "2026-03-02T15:23:26Z",
  "credentials": {
    "account": "pairing-flow",
    "namespace": "default",
    "sessionPrefix": "auth-group"
  },
  "toolsUsed": [
    "curl",
    "python3"
  ],
  "assertions": [
    {
      "id": "VAL-AUTH-001",
      "title": "Pair start returns code and expiry",
      "status": "pass",
      "steps": [
        {
          "action": "POST /api/pair/start",
          "expected": "HTTP 200 with pairingId, 6-digit code, and expiresAt",
          "observed": "HTTP 200 with pairingId=pair_ae0424bb-ef15-4502-a859-43eeb3cb4126, code=369355 (len=6), expiresAt=1772465254202"
        }
      ],
      "evidence": {
        "network": "POST /api/pair/start -> 200",
        "responseSummary": "pairingId present, code matched 6-digit format, expiresAt numeric"
      },
      "issues": null
    },
    {
      "id": "VAL-AUTH-002",
      "title": "Pair confirm with valid code issues tokens",
      "status": "pass",
      "steps": [
        {
          "action": "POST /api/pair/confirm with pairingId + code",
          "expected": "HTTP 200 with accessToken, refreshToken, expiresAt, deviceId",
          "observed": "HTTP 200 with keys accessToken, refreshToken, expiresAt, deviceId (accessTokenLen=39, refreshTokenLen=39, deviceId=device_1c9d436d-35eb-4cee-bfee-0d0bbf3e9fe9)"
        }
      ],
      "evidence": {
        "network": "POST /api/pair/confirm -> 200",
        "responseSummary": "Response included accessToken, refreshToken, expiresAt, deviceId"
      },
      "issues": null
    },
    {
      "id": "VAL-AUTH-003",
      "title": "Pair confirm rejects invalid code",
      "status": "pass",
      "steps": [
        {
          "action": "POST /api/pair/confirm with wrong code",
          "expected": "HTTP 401 with error body",
          "observed": "HTTP 401 with {code: 'invalid_pairing_code', message: 'Pairing code is invalid'}"
        }
      ],
      "evidence": {
        "network": "POST /api/pair/confirm (wrong code) -> 401",
        "responseBody": "{code: 'invalid_pairing_code', message: 'Pairing code is invalid'}"
      },
      "issues": null
    },
    {
      "id": "VAL-AUTH-004",
      "title": "Pair confirm rejects expired code",
      "status": "pass",
      "steps": [
        {
          "action": "Review unit test coverage for expiry",
          "expected": "Expired pairing code returns 410/expired error",
          "observed": "packages/mobile-gateway/src/__tests__/auth.test.ts asserts 410 with code 'pairing_code_expired' using Date.now override"
        }
      ],
      "evidence": {
        "unitTest": "auth.test.ts: 'POST /api/pair/confirm rejects expired code with 410'"
      },
      "issues": null
    },
    {
      "id": "VAL-AUTH-005",
      "title": "Valid token authenticates requests",
      "status": "pass",
      "steps": [
        {
          "action": "GET /api/workspaces with Bearer access token",
          "expected": "HTTP 200 with workspace list",
          "observed": "HTTP 200 with [{id:'default',name:'Default Workspace'}]"
        }
      ],
      "evidence": {
        "network": "GET /api/workspaces (with Bearer token) -> 200",
        "responseBody": "[{id:'default',name:'Default Workspace'}]"
      },
      "issues": null
    },
    {
      "id": "VAL-AUTH-006",
      "title": "Expired token rejected",
      "status": "pass",
      "steps": [
        {
          "action": "Review unit test coverage for expired access token",
          "expected": "Expired access token returns 401 with token_expired",
          "observed": "auth.test.ts asserts 401 with {code:'token_expired', message:'Access token expired'} after Date.now > expiresAt"
        }
      ],
      "evidence": {
        "unitTest": "auth.test.ts: 'expired access token returns 401 with token_expired'"
      },
      "issues": null
    },
    {
      "id": "VAL-AUTH-007",
      "title": "Device revocation invalidates all tokens",
      "status": "pass",
      "steps": [
        {
          "action": "POST /api/devices/{deviceId}/revoke with valid token",
          "expected": "HTTP 200",
          "observed": "HTTP 200 with {status:'ok'}"
        },
        {
          "action": "GET /api/workspaces with revoked token",
          "expected": "HTTP 401/403",
          "observed": "HTTP 401 with {code:'unauthorized', message:'Authorization required'}"
        }
      ],
      "evidence": {
        "network": "POST /api/devices/device_1c9d436d-35eb-4cee-bfee-0d0bbf3e9fe9/revoke -> 200; GET /api/workspaces (same token) -> 401"
      },
      "issues": null
    },
    {
      "id": "VAL-AUTH-008",
      "title": "Token refresh exchange",
      "status": "pass",
      "steps": [
        {
          "action": "POST /api/pair/refresh with valid refresh token",
          "expected": "HTTP 200 with new accessToken + expiresAt",
          "observed": "HTTP 200 with {accessToken: '***', expiresAt: 1772465864351}"
        },
        {
          "action": "POST /api/pair/refresh with invalid refresh token",
          "expected": "HTTP 401",
          "observed": "HTTP 401 with {code:'invalid_refresh_token', message:'Refresh token is invalid or expired'}"
        }
      ],
      "evidence": {
        "network": "POST /api/pair/refresh (valid) -> 200; POST /api/pair/refresh (invalid) -> 401"
      },
      "issues": null
    },
    {
      "id": "VAL-GW-013",
      "title": "Protected endpoints require auth",
      "status": "pass",
      "steps": [
        {
          "action": "GET /api/workspaces without auth",
          "expected": "HTTP 401",
          "observed": "HTTP 401 with {code:'unauthorized', message:'Authorization required'}"
        },
        {
          "action": "GET /api/workspaces/default/sessions without auth",
          "expected": "HTTP 401",
          "observed": "HTTP 401 with {code:'unauthorized', message:'Authorization required'}"
        },
        {
          "action": "GET /api/sessions/seeded-session-1 without auth",
          "expected": "HTTP 401",
          "observed": "HTTP 401 with {code:'unauthorized', message:'Authorization required'}"
        },
        {
          "action": "POST /api/sessions/seeded-session-1/messages without auth",
          "expected": "HTTP 401",
          "observed": "HTTP 401 with {code:'unauthorized', message:'Authorization required'}"
        },
        {
          "action": "DELETE /api/sessions/seeded-session-1 without auth",
          "expected": "HTTP 401",
          "observed": "HTTP 401 with {code:'unauthorized', message:'Authorization required'}"
        }
      ],
      "evidence": {
        "network": "All tested protected endpoints returned 401 without Authorization header"
      },
      "issues": null
    }
  ],
  "frictions": [],
  "blockers": [],
  "summary": "Tested 9 assertions: all passed; expiry assertions validated via unit tests in auth.test.ts."
}
